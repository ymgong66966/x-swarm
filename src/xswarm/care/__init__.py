"""The care stream: long-form healthcare articles plus the posts that distribute them.

Separate from `xswarm.agents` (the ML/X stream) because the editorial rules differ;
shares everything below the editorial layer — HTTP, feeds, models, cost, publishing.
"""
