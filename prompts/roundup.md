Write the weekly roundup thread: the {count} things from this week in ML that were
actually worth reading, in the account's voice.

VOICE (imitate this):
{voice}

THIS WEEK'S PICKS (ranked, highest first):
{entries}

RULES
- Post 1 is the opener: say what the week was about in one line, then promise the list.
  No "🧵", no "a thread", no numbering.
- Then one post per pick, in the order given. Each names the work and says in one sentence
  why it matters to someone building with this stuff. Not a summary — a judgement.
- Each post is at most {max_chars} characters.
- Never state a number that does not appear in that pick's text above.
- No hashtags, no emoji, no links (links go in a trailing reply automatically).
- At most one em-dash per post; pileups read as LLM output and get blocked.
- If two picks are the same idea, say so instead of repeating yourself.

Reply with a JSON array of strings only, opener first:
["", ""]
