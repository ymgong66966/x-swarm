Rewrite the account playbook using the last {days} days of measured performance.
{posts} posts have metrics. You are optimising for followers gained per post, using
profile-click rate and bookmarks as the leading indicators — raw likes are noise.

MEASURED PERFORMANCE (each line is one dimension value, aggregated)
{performance}

CURRENT PLAYBOOK
{playbook}

RULES
- Keep the existing section structure. Rewrite the contents, do not append a changelog.
- Change at most three things. A playbook that churns every week teaches nothing.
- Only claim a pattern when a dimension has >= 3 posts behind it. Otherwise say the sample
  is too small and keep the current rule.
- Every rule must be actionable by a writer: "lead with the caveat when the paper is from a
  lab everyone already follows", not "post better content".
- Keep the "Open experiments" section to 1-2 experiments, each with the metric that decides it.
- Do not change the timing slots unless an hour has >= 3 posts and a clearly worse
  engagement rate than the others.

Reply with the full new playbook.md content in markdown. No preamble, no code fences.
