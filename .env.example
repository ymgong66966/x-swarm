# All settings are optional; defaults let the pipeline run with `--dry-run`.

# Postgres in production; defaults to a local SQLite file.
# XSWARM_DATABASE_URL=postgresql+psycopg://user:pass@host:5432/xswarm

# Needed for anything other than --dry-run. Set either provider; OpenAI wins when both
# are present unless XSWARM_LLM_PROVIDER is set explicitly to "anthropic".
# XSWARM_OPENAI_API_KEY=
# XSWARM_ANTHROPIC_API_KEY=
# XSWARM_LLM_PROVIDER=auto

# Publishing. Without a key, `xswarm publish` records what it would schedule and stops.
# The social set is discovered automatically when you have exactly one.
# XSWARM_TYPEFULLY_API_KEY=
# XSWARM_TYPEFULLY_SOCIAL_SET_ID=
# XSWARM_PUBLISH_TIMEZONE=America/New_York
# XSWARM_PUBLISH_SLOTS=["08:45","12:30","17:00"]
# Pillars allowed to skip human approval. Empty = everything is reviewed.
# XSWARM_AUTOPUBLISH_PILLARS=[]

# Cost control. Prices are USD per million tokens as [prompt, completion].
# XSWARM_MONTHLY_BUDGET_USD=20
# XSWARM_MODEL_PRICES={"gpt-4.1":[2.0,8.0]}
# XSWARM_IMAGE_PRICES={"gpt-image-1":0.063}

# Visuals. auto = generate art only where there is no measured number to plot;
# render = matplotlib templates only; generate = always try generated art first.
# XSWARM_VISUAL_MODE=auto
# XSWARM_IMAGE_MODEL=gpt-image-1
# XSWARM_IMAGE_SIZE=1536x1024
# XSWARM_IMAGE_QUALITY=medium
# XSWARM_DEFAULT_ART_STYLE=frontier_diagram

# Your own material: how much of a fetched page or paper is kept as grounding.
# XSWARM_INGEST_MAX_CHARS=24000

# Threads. Pillars listed here get expanded into multi-post threads when the brief
# has at least XSWARM_THREAD_MIN_CLAIMS grounded claims.
# XSWARM_THREAD_PILLARS=["paper_of_the_day","explainer"]
# XSWARM_MAX_THREAD_POSTS=5
# XSWARM_ROUNDUP_PICKS=5

# Optional, raise rate limits / unlock enrichment.
# XSWARM_SEMANTIC_SCHOLAR_API_KEY=
# XSWARM_GITHUB_TOKEN=
# XSWARM_HF_TOKEN=

# --- Care stream (healthcare articles) -------------------------------------
# The site the product facts are read from, robots.txt permitting.
# XSWARM_CARE_SITE_URL=https://alvernahealth.com
# XSWARM_CARE_SITE_PATHS=["/","/providers","/become-a-trainer"]
# XSWARM_CARE_BLOG_BASE_URL=https://alvernahealth.com/blog
# XSWARM_CARE_ARTICLES_PER_RUN=2
# XSWARM_CARE_MIN_WORDS=850
# XSWARM_CARE_MAX_WORDS=1500
# XSWARM_CARE_PROMOS_PER_ARTICLE=3

# Analytics for the two-stream dashboard. Without these, traffic columns stay zero.
# XSWARM_PLAUSIBLE_API_KEY=
# XSWARM_PLAUSIBLE_SITE_ID=alvernahealth.com
# XSWARM_DASHBOARD_LOOKBACK_DAYS=30
